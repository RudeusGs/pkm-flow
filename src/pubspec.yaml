name: block_based_pkm_flutter
description: "Mobile Flutter client for Block Based PKM."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  dio: ^5.9.2
  flutter_secure_storage: ^9.2.4
  signalr_netcore: ^1.4.4
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
